# Claude Code instructions

Read and follow `AGENTS.md` as the canonical repository instructions. Then read `skills/video-voiceover/SKILL.md` before creating or changing a video.

The implementation and verification commands are intentionally shared with Codex. If either instruction file changes, keep the other surface aligned and do not fork the workflow into agent-specific implementations.
